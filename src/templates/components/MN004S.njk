{% set menuData004 = [
  {
    name: "낱장인쇄(제본없이)",
    subs: ["낱장인쇄(제본없이) 안내 문구 1", "낱장인쇄(제본없이) 안내 문구 2"],
    itemList: [
      { fileName: "A014-01.jpg", alt: "낱장인쇄(제본없이) 이미지 1" },
      { fileName: "A014-02.jpg", alt: "낱장인쇄(제본없이) 이미지 2" }
    ],
    tooltipList: []
  },
  {
    name: "소량책자표지",
    subs: ["소량책자표지 안내 문구 1", "소량책자표지 안내 문구 2"],
    itemList: [
      { fileName: "A015-01.jpg", alt: "소량책자표지 이미지 1" },
      { fileName: "A015-02.jpg", alt: "소량책자표지 이미지 2" }
    ],
    tooltipList: []
  },
  {
    name: "소량전단",
    subs: ["소량전단 안내 문구 1", "소량전단 안내 문구 2"],
    itemList: [
      { fileName: "A016-01.jpg", alt: "소량전단 이미지 1" },
      { fileName: "A016-02.jpg", alt: "소량전단 이미지 2" }
    ],
    tooltipList: []
  }
] %}
<script id="menu-data-004" type="application/json" hidden>
  {{ menuData004 | dump | safe }}
</script>
<nav class="all-menu-wrap" data-menu-index="2">
  <div class="all-menu">
    <div class="category-wrap">
      <div class="category">
        <strong class="depth-1">출력</strong>
        <ul>
          <li>
            <p class="depth-2">출력</p>
            <ul>
              {% for i in range(0,3) %}
                <li class="depth-3">
                  <a href="#" data-index="{{ i }}">
                    {{ menuData004[i].name }}
                    {% for templateEl in menuData004[i].tooltipList %}
                      {{ templateEl | safe }}
                    {% endfor %}
                  </a>
                </li>
              {% endfor %}
            </ul>
          </li>
        </ul>
      </div>
    </div>
    <div class="header-product-wrap">
      <ul class="product-img">
        {% for i in [0,1] %}
          <li>
            <img data-img-index="{{ i }}" src="/img/product/{{ menuData004[0].itemList[i].fileName }}" alt="{{ menuData004[0].itemList[i].alt }}"/>
          </li>
        {% endfor %}
      </ul>
      <div class="product-info">
        <strong class="product-name" data-name>
          {{ menuData004[0].name }}
          {% for templateEl in menuData004[0].tooltipList %}
            {{ templateEl | safe }}
          {% endfor %}
        </strong>
        {% for k in [0,1] %}
          <p class="guide-sub --small" data-sub-index="{{ k }}">
            {{ menuData004[0].subs[k] }}
          </p>
        {% endfor %}
      </div>
    </div>
  </div>
</nav>